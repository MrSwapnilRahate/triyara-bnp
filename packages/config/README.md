# @triyara/config

Shared build configuration: the Tailwind preset (brand tokens) consumed by apps.
TypeScript base config lives at the repo root (`tsconfig.base.json`); ESLint is the
root flat config (`eslint.config.js`).
